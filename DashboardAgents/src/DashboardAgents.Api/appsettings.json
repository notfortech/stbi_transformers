{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Llm": {
    "_comment": "Set Provider to \"openai\" (default) or \"anthropic\". Override with LLM_PROVIDER env var.",
    "Provider": "openai"
  },

  "OpenAI": {
    "_comment": "Never commit a real key. Set OPENAI_API_KEY as an environment variable or in Azure Key Vault.",
    "ApiKey": "",
    "Model": "gpt-4o",
    "MaxTokens": 8000,
    "Temperature": 0.2,
    "BaseUrl": "https://api.openai.com/v1/chat/completions"
  },

  "Anthropic": {
    "_comment": "Optional. Only activated when Llm:Provider is \"anthropic\" AND ApiKey is set. Set ANTHROPIC_API_KEY env var.",
    "ApiKey": "",
    "Model": "claude-sonnet-4-6",
    "MaxTokens": 8000,
    "BaseUrl": "https://api.anthropic.com/v1/messages",
    "ApiVersion": "2023-06-01"
  },

  "Koru": {
    "_comment": "Base URL of the koru-main backend. Set KORU_BASE_URL and KORU_API_KEY env vars.",
    "BaseUrl": "",
    "ServiceApiKey": "",
    "TimeoutSeconds": 30
  },

  "Jwt": {
    "_comment": "Must match the JWT signing key used by koru-main. Set JWT_KEY, JWT_ISSUER, JWT_AUDIENCE env vars.",
    "Key": "",
    "Issuer": "",
    "Audience": ""
  },

  "Redis": {
    "_comment": "Azure Cache for Redis connection string. Set REDIS_CONNECTION env var. When absent, falls back to in-memory stores.",
    "ConnectionString": ""
  },

  "KeyVault": {
    "_comment": "Optional Azure Key Vault URL (e.g. https://my-vault.vault.azure.net/). Set KEY_VAULT_URL env var. When set, secrets are pulled from Key Vault at startup using Managed Identity.",
    "VaultUrl": ""
  },

  "Swagger": {
    "_comment": "Set to true to expose Swagger UI in production (disabled by default). Enabled automatically in Development.",
    "Enabled": false
  },

  "Cors": {
    "_comment": "Set CORS_ORIGINS env var to the production frontend URL (e.g. https://app.studiotechbi.com). Multiple origins: add Cors:AllowedOrigins:1, :2, etc. as separate App Service settings.",
    "AllowedOrigins": [
      "http://localhost:3000",
      "https://localhost:3000",
      "http://localhost:5173",
      "https://localhost:5173"
    ]
  }
}
